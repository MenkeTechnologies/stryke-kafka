name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            cross: true
          - os: macos-latest
            target: x86_64-apple-darwin
          - os: macos-latest
            target: aarch64-apple-darwin
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2

      - name: Install cmake / musl-tools
        run: |
          if [ "$RUNNER_OS" = "Linux" ]; then
            sudo apt-get update && sudo apt-get install -y cmake
            if [[ "${{ matrix.target }}" == *musl ]]; then
              sudo apt-get install -y musl-tools
            fi
          fi

      - name: Install cross
        if: matrix.cross
        run: cargo install cross --locked

      - name: Build (native)
        if: '!matrix.cross'
        run: cargo build --release --locked --target ${{ matrix.target }}

      - name: Build (cross)
        if: matrix.cross
        run: cross build --release --locked --target ${{ matrix.target }}

      - name: Package
        shell: bash
        run: |
          set -euo pipefail
          cd target/${{ matrix.target }}/release
          strip stryke-kafka-helper 2>/dev/null || true
          tar czf ../../../stryke-kafka-${{ github.ref_name }}-${{ matrix.target }}.tar.gz stryke-kafka-helper
          cd ../../..
          sha256sum stryke-kafka-${{ github.ref_name }}-${{ matrix.target }}.tar.gz \
              > stryke-kafka-${{ github.ref_name }}-${{ matrix.target }}.tar.gz.sha256

      - uses: actions/upload-artifact@v6
        with:
          name: stryke-kafka-${{ matrix.target }}
          path: |
            stryke-kafka-${{ github.ref_name }}-${{ matrix.target }}.tar.gz
            stryke-kafka-${{ github.ref_name }}-${{ matrix.target }}.tar.gz.sha256

  publish:
    name: Publish Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v6

      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create ${{ github.ref_name }} \
            --title "${{ github.ref_name }}" \
            --generate-notes \
            artifacts/*.tar.gz \
            artifacts/*.tar.gz.sha256
